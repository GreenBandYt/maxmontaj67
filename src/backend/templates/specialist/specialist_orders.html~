{% extends "base.html" %}

{% block title %}Специалист: Заказы{% endblock %}

{% block content %}
<div class="specialist-orders">
    <!-- Панель навигации -->
    <div class="navigation-panel">
<!--        <a href="{{ url_for('specialist.orders') }}" class="button">Мои заказы</a>-->
        <a href="{{ url_for('logout') }}" class="button logout">Выход</a>
    </div>

    <!-- Заголовок страницы -->
    <h2>Список заказов для специалиста</h2>

    <!-- Таблица заказов с фиксированным заголовком -->
    <div class="scrollable-table">
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Описание</th>
                    <th>Статус</th>
                    <th>Дата создания</th>
                    <th>Дата нового обновления</th>
                    <th>Дедлайн</th>
                    <th>Дата монтажа</th>
                </tr>
            </thead>
            <tbody>
                {% for order in orders %}
                <tr>
                    <td>{{ order.id }}</td>
                    <td>{{ order.description }}</td>
                    <td>{{ order.status }}</td>
                    <td>{{ order.created_at.strftime('%Y-%m-%d') }}</td>
                    <td>{{ order.updated_at.strftime('%Y-%m-%d') }}</td>
                    <td>{{ order.deadline_at.strftime('%Y-%m-%d') if order.deadline_at else "Не указан" }}</td>
                    <td>{{ order.montage_date.strftime('%Y-%m-%d') if order.montage_date else "Не указана" }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>

    </div>
</div>
{% endblock %}
