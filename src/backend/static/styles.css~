/* Общие стили */
body {
    font-family: Arial, sans-serif;
    background-color: #b2babb;
    color: #333;
    margin: 0;
    padding: 0;
    line-height: 1.6;
}

h1 {
    text-align: center;
    color: #4CAF50; /* Цвет заголовка */
    margin-top: 10px;
    font-size: 1.8rem;
}

/* Основной контейнер */
main {
    max-width: 1200px;
    margin: 20px auto;
    padding: 20px;
    background-color: #fff;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Лёгкая тень */
    border-radius: 5px;
}

/* Футер */
footer {
    background: #333; /* Тёмный фон */
    color: #fff; /* Белый текст */
    padding: 10px 20px;
    text-align: center;
    font-size: 0.9rem;
    position: fixed;
    bottom: 0;
    width: 100%;
    box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1); /* Тень над футером */
}

footer p {
    margin: 5px 0;
}

/* Ссылки */
a {
    color: #4CAF50;
    text-decoration: none;
    font-weight: bold;
}

a:hover {
    text-decoration: underline;
    color: #45a049; /* Тёмно-зелёный при наведении */
}

/* Кнопки */
button {
    background-color: #4CAF50;
    color: white;
    border: none;
    padding: 10px 15px;
    text-transform: uppercase;
    font-size: 16px;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #45a049;
}

/* Кнопка выхода */
.logout-button {
    background-color: #f44336;
    color: white;
    padding: 10px 20px;
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    transition: background-color 0.3s ease;
    position: absolute;
    right: 20px;
    top: 20px;
}

.logout-button:hover {
    background-color: #d32f2f;
}

/* Таблицы */
table {
    width: 100%;
    border-collapse: collapse;
    margin: 20px 0;
    font-size: 1rem;
}

table th,
table td {
    border: 1px solid #ddd;
    padding: 10px;
    text-align: left;
}

table th {
    background: linear-gradient(to bottom, #4CAF50, #45a049);
    color: white;
    text-transform: uppercase;
}

table tr:nth-child(even) {
    background-color: #f9f9f9;
}

table tr:hover {
    background-color: #f1f1f1;
}

/* Мобильная адаптивность */
@media (max-width: 768px) {
    main {
        padding: 10px;
    }

    table th,
    table td {
        font-size: 0.9rem;
        padding: 8px;
    }

    button,
    .logout-button {
        font-size: 14px;
        padding: 8px 10px;
    }
}

.navigation-panel {
    display: flex;
    justify-content: space-around;
    margin-bottom: 20px;
}

.navigation-panel .button {
    background-color: #4CAF50;
    color: white;
    text-decoration: none;
    padding: 10px 15px;
    border-radius: 5px;
    font-size: 16px;
    transition: background-color 0.3s ease;
}

.navigation-panel .button:hover {
    background-color: #45a049;
}
