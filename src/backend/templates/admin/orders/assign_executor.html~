{% extends "base.html" %}

{% block title %}Назначить исполнителя{% endblock %}

{% block content %}
<h2>Назначить исполнителя для заказа ID: {{ order.id }}</h2>
<p><strong>Описание:</strong> {{ order.description }}</p>
<p><strong>Клиент:</strong> {{ order.customer_name }}</p>

<form method="POST">
    <label for="executor_id">Выберите исполнителя:</label>
    <select name="executor_id" id="executor_id" required>
        {% for executor in executors %}
        <option value="{{ executor.id }}">{{ executor.name }}</option>
        {% endfor %}
    </select>
    <button type="submit" class="button">Назначить</button>
</form>
<a href="{{ url_for('admin.orders') }}" class="button">Отмена</a>
{% endblock %}

