/* Стили для панели администратора */
.admin-panel {
    padding: 20px;
    background-color: #ffffff;
    border: 1px solid #ddd;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
}

/* Кнопки администратора */
.admin-panel .button {
    background-color: #f44336;
    color: white;
    border: none;
    padding: 10px 20px;
    font-size: 16px;
    border-radius: 5px;
    cursor: pointer;
}

.admin-panel .button:hover {
    background-color: #d32f2f;
}

/* Таблицы администратора */
.admin-table th {
    background-color: #f44336;
    color: white;
    text-align: center;
}

.admin-table td {
    border: 1px solid #ddd;
    padding: 8px;
    text-align: left;
}

.admin-table tr:nth-child(even) {
    background-color: #f9f9f9;
}

.admin-table tr:hover {
    background-color: #f1f1f1;
}

/* Чёткое указание для администраторских таблиц */
.admin-panel .admin-table {
    font-size: 14px;
}

/* Кнопки внутри администраторских таблиц */
.admin-table a.button {
    background-color: #f44336;
    color: white;
    text-decoration: none;
    padding: 5px 10px;
    border-radius: 5px;
    font-size: 12px;
    transition: background-color 0.3s ease;
}

.admin-table a.button:hover {
    background-color: #d32f2f;
}
